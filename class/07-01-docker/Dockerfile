# 컴퓨터 만드는 설명서  (이컴퓨터 안에서 실행될 명령어 작성하기)


# 1. 운영체제 설치 (node 14 버전, npm, yarn 모두 설치되어있는 리눅스)

# FROM ubuntu:22.04
FROM node:14

# 3. 내컴퓨터에 있는 폴더나 파일을 도커 컴퓨터 안으로 복사하기
# COPY 내컴퓨터 폴더 위치 / 도커 컴퓨터 폴더 위치
# 현재위치의 index.js 를 / 도커 컴터에 카피할꺼야
COPY ./index.js /

# ---------------최적화된 압축파일을 "이미지"라고한다. 이미지까지 준비한상태 
# --------------- 그후 CMD실행함.(CMD 가장마지막에 실행됨 )

# 4. 도커 안에서 index.js 실행시키기
CMD node index.js

# CMD yarn start:dev







