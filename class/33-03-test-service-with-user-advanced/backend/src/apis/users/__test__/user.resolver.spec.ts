describe('UsersResolver', () => {
  beforeEach(() => {
    // 로그인 등 공통 기능
  });

  describe('fetchUser', () => {
    it('유저정보 꺼내오기 ', () => {
      // 컨트롤러와 같은 방식으로 테스트 하기
    });
  });

  describe('createUser', () => {
    it('유저 등록하기 ', () => {
      // 컨트롤러와 같은 방식으로 테스트 하기
    });
  });
});
